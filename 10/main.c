#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>

int kmp(char* origin, char *needle)
{

}

bool isMatch(char* s, char* p) {
    int length;
}

int main()
{
    char* s[] = "";
    char* p[] = "";
    int result;
    result = isMatch(s, p);
    printf("%d\n", result);
    return 0;
}
